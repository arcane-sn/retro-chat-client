# Loaded by `vite build`. The Socket.IO server URL (Cloudflare Tunnel -> laptop).
# Not secret — it's a public endpoint. Baked into the bundle at build time.
VITE_SERVER_URL=https://chat-server.arcane-magus.site
